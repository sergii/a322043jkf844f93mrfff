enforce_dependencies: strict
dependencies:
  - "."
metadata:
  bounded_context: workspace
  migration_note: "Temporary dependency on the legacy donor shell while Organization and shared Rails primitives are still extracted."
