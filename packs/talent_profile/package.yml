enforce_dependencies: strict
dependencies:
  - packs/platform
  - packs/workspace
metadata:
  bounded_context: talent_profile
  owns:
    - Candidate
    - CandidateProfileVersion
    - CandidateEvidence
